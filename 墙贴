Sub 墙贴()
Application.ScreenUpdating = False
Dim hang, senu, hangs, ii, sh, e, f As Integer

Dim arr2 As Variant

Dim sjy, a, b, c, d As String

hang = Selection.Row

ii = Selection.Column
senu = Selection.Rows.Count


hangs = hang + senu - 1

On Error Resume Next


'MsgBox senu
If LBound(arr3) > UBound(arr3) Then   '判断数组是否为空


arr3 = Selection.Value
Else
'ActiveSheet.Range("ab1").Resize(UBound(arr), 1) = Application.Index(arr, , 1)
'appliction.SendKeys

'Dim stemp As String

'stemp = Join(Application.Index(arr3, , 1))
'stemp = Trim(stemp)
'arr2 = Split(stemp)

'arr2 = Split(appliction.Trim(Join(arr3)))


ActiveSheet.Cells(hang, ii).Resize(UBound(arr3), 1) = Application.Index(arr3, , 1)




Set arr3 = Nothing
End If
'Range("ab1").Resize(UBound(arr)) = arr




End Sub
